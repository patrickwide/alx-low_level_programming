Here is a list of C functions that demonstrate recursion. 

- `0-puts_recursion.c`: This function takes a string as input and prints it to the standard output, followed by a new line character, using recursion.
- `1-print_rev_recursion.c`: This function takes a string as input and prints it to the standard output in reverse order, using recursion.
- `2-strlen_recursion.c`: This function takes a string as input and returns its length, using recursion.
- `3-factorial.c`: This function takes an integer as input and returns its factorial value, using recursion.
- `4-pow_recursion.c`: This function takes two integers as input, x and y, and returns the value of x raised to the power of y, using recursion.
- `5-sqrt_recursion.c`: This function takes an integer as input and returns its natural square root, using recursion.
- `6-is_prime_number.c`: This function takes an integer as input and returns 1 if it is a prime number, and 0 otherwise, using recursion.
- `100-is_palindrome.c`: This function takes a string as input and returns 1 if it is a palindrome (i.e., reads the same backwards as forwards), and 0 otherwise, using recursion.
- `101-wildcmp.c`: This function takes two strings as input and returns 1 if they can be considered identical (i.e., matching character by character with the exception of the special character '*'), and 0 otherwise, using recursion.

